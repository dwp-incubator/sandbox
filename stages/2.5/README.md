#2.5 Introduce npm and import express

1. Initialize the project with `npm`.
2. Add `node_modules` path into the `.gitignore` file.
3. Use `npm` to add express as a dependency
4. Review the `package.json` file

